import {
  Flex,
  Text,
  Icon,
  HStack,
  Box,
  Link,
  GridItem,
} from '@chakra-ui/react';

export function MarketPairs() {
  return (
    <Flex
      w="100%"
      maxWidth={370}
      borderTopWidth={1}
      borderBottomWidth={1}
      borderRightWidth={1}
      borderColor="gray.700"
    >
      Market Pair
    </Flex>
  );
}
